interface:
  display_name: "Frontend Task"
  short_description: "Preflight sources and prepare frontend work"
  brand_color: "#57D6B6"
  default_prompt: "Use $frontend-task to prepare or continue this frontend task from the current worktree and relevant evidence, asking only for material decisions."
